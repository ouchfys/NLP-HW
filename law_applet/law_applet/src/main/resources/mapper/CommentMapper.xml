<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CommentMapper">

    <select id="getByLawId" resultType="com.example.vo.CommentVO">
        select a.*,c.user_name from t_comment a
            left join t_order b on b.id = a.order_id
            left join t_user c on c.id = b.user_id
        where b.lawyer_user_id = #{id} and a.status != 1
    </select>

    <select id="commentPage" resultType="com.example.vo.CommentVO">
        select a.*, b.order_no from t_comment a left join t_order b on a.order_id = b.id
    </select>
</mapper>
